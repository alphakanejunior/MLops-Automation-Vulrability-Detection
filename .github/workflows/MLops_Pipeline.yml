name: Full Project Automatisation de la gestion des vulnérabilités dans les pipelines MLops

# =============================================================
# Triggers du workflow
# =============================================================
on:
  push:
    branches:
      - Develop
  pull_request:
    branches:
      - Develop

# =============================================================
# Variables globales
# =============================================================
env:
  # Identifiant unique pour versionner tous les rapports
  TIMESTAMP: ${{ github.run_id }}

jobs:
  # =============================================================
  # Job 1 : CodeScan
  # Objectif : Analyser les vulnérabilités dans le code source
  # Outil    : Bandit (Python SAST)
  # =============================================================
  CodeScan:
    name: Code Security Scan (Bandit)
    runs-on: ubuntu-latest

    steps:
      # ---------------------------------------------------------
      # Étape 1 : Récupération du code source
      # ---------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # Étape 2 : Configuration de l’environnement Python
      # ---------------------------------------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ---------------------------------------------------------
      # Étape 3 : Installation des outils nécessaires
      # - Bandit  : scan de sécurité du code
      # - tabulate: affichage structuré des résultats
      # ---------------------------------------------------------
      - name: Install Bandit and dependencies
        run: |
          python -m pip install --upgrade pip
          pip install bandit tabulate

      # ---------------------------------------------------------
      # Étape 4 : Exécution du scan Bandit
      # - Scan récursif du projet
      # - Génération d’un rapport JSON versionné
      # - Le pipeline continue même si des vulnérabilités sont trouvées
      # ---------------------------------------------------------
      - name: Run Bandit Scan
        run: |
          REPORT_DIR="reports/bandit/${{ env.TIMESTAMP }}"
          mkdir -p "$REPORT_DIR"
          bandit -r . -f json -o "$REPORT_DIR/bandit-report.json" || true
          echo "✅ Bandit scan finished"

      # ---------------------------------------------------------
      # Étape 5 : Affichage des résultats Bandit dans la console
      # - Lecture du rapport JSON
      # - Présentation sous forme de tableau lisible
      # ---------------------------------------------------------
      - name: Display Bandit Scan Results
        run: |
          python - << EOF
          import json
          from tabulate import tabulate

          report_file = "reports/bandit/${{ env.TIMESTAMP }}/bandit-report.json"
          with open(report_file) as f:
              data = json.load(f)

          vulns = data.get("results", [])
          if not vulns:
              print("✅ No issues found by Bandit")
          else:
              table = []
              for v in vulns:
                  table.append([
                      v.get("filename"),
                      v.get("line_number"),
                      v.get("issue_severity"),
                      v.get("issue_confidence"),
                      v.get("issue_text")
                  ])
              print("\n🔍 Bandit Scan Results:")
              print(tabulate(
                  table,
                  headers=["File", "Line", "Severity", "Confidence", "Issue"],
                  tablefmt="github"
              ))
          EOF

      # ---------------------------------------------------------
      # Étape 6 : Archivage du rapport Bandit
      # ---------------------------------------------------------
      - name: Upload Bandit Report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report-${{ env.TIMESTAMP }}
          path: reports/bandit/${{ env.TIMESTAMP }}/bandit-report.json
          retention-days: 30

  # =============================================================
  # Job 2 : DependencyScan
  # Objectif : Analyser les vulnérabilités des dépendances
  # Outil    : Trivy (filesystem scan)
  # =============================================================
  DependencyScan:
    name: Dependency Vulnerability Scan (Trivy)
    runs-on: ubuntu-latest
    needs: [CodeScan]

    steps:
      # ---------------------------------------------------------
      # Étape 1 : Récupération du code source
      # ---------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # Étape 2 : Installation de Trivy
      # ---------------------------------------------------------
      - name: Install Trivy
        run: |
          sudo apt-get update
          sudo apt-get install -y wget
          wget https://github.com/aquasecurity/trivy/releases/latest/download/trivy_0.68.2_Linux-64bit.deb
          sudo dpkg -i trivy_0.68.2_Linux-64bit.deb
          trivy --version

      # ---------------------------------------------------------
      # Étape 3 : Scan des dépendances
      # - Analyse des fichiers requirements / poetry / pipenv
      # - Génération d’un rapport JSON
      # ---------------------------------------------------------
      - name: Run Trivy Dependency Scan
        run: |
          REPORT_DIR="reports/dependency/${{ env.TIMESTAMP }}"
          mkdir -p "$REPORT_DIR"

          trivy fs \
            --scanners vuln \
            --format json \
            --output "$REPORT_DIR/trivy-dependency-report.json" \
            . || true

          echo "✅ Trivy dependency scan finished"

      # ---------------------------------------------------------
      # Étape 4 : Installation des dépendances Python (affichage)
      # ---------------------------------------------------------
      - name: Install Python display dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tabulate

      # ---------------------------------------------------------
      # Étape 5 : Affichage des résultats Trivy dans la console
      # ---------------------------------------------------------
      - name: Display Dependency Scan Results
        run: |
          python - << 'EOF'
          import json
          from tabulate import tabulate

          report = "reports/dependency/${{ env.TIMESTAMP }}/trivy-dependency-report.json"
          with open(report) as f:
              data = json.load(f)

          rows = []
          for result in data.get("Results", []):
              target = result.get("Target", "")
              for vuln in result.get("Vulnerabilities", []) or []:
                  rows.append([
                      target,
                      vuln.get("PkgName"),
                      vuln.get("InstalledVersion"),
                      vuln.get("VulnerabilityID"),
                      vuln.get("Severity"),
                      vuln.get("Title", "")[:80]
                  ])

          if not rows:
              print("✅ No dependency vulnerabilities found")
          else:
              print("\n📦 Dependency Scan Results (Trivy):")
              print(tabulate(
                  rows,
                  headers=["Target", "Package", "Version", "CVE", "Severity", "Description"],
                  tablefmt="github"
              ))
          EOF

      # ---------------------------------------------------------
      # Étape 6 : Archivage du rapport Trivy
      # ---------------------------------------------------------
      - name: Upload Dependency Scan Report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-dependency-report-${{ env.TIMESTAMP }}
          path: reports/dependency/${{ env.TIMESTAMP }}/trivy-dependency-report.json
          retention-days: 30

  # =============================================================
  # Job 4 : ModelScan
  # Objectif : Scanner tous les fichiers modèles ML et générer un JSON
  # =============================================================
# =============================================================
# Job 3 : ModelScan officiel (corrigé et fonctionnel)
# =============================================================
  ModelScan:
    name: Model Serialization Security Scan
    runs-on: ubuntu-latest
    needs: DependencyScan

    steps:
      # ---------------------------------------------------------
      # Étape 1 : Récupérer le dépôt
      # ---------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # Étape 2 : Installer ModelScan
      # ---------------------------------------------------------
      - name: Install ModelScan
        run: |
          python -m pip install --upgrade pip
          pip install modelscan==0.8.7

      # ---------------------------------------------------------
      # Étape 3 : Créer dossier des rapports
      # ---------------------------------------------------------
      - name: Create Reports Folder
        run: mkdir -p "reports/models/${{ env.TIMESTAMP }}/official"

      # ---------------------------------------------------------
      # Étape 4 : Exécuter ModelScan et convertir TXT -> JSON
      # ---------------------------------------------------------
      - name: Run ModelScan and generate JSON compatible CVEEnrichment
        run: |
          REPORT_TXT="reports/models/${{ env.TIMESTAMP }}/official/modelscan-report.txt"
          REPORT_JSON="reports/models/${{ env.TIMESTAMP }}/official/modelscan-report.json"

          mkdir -p "$(dirname "$REPORT_TXT")"

          # Run ModelScan and save TXT
          modelscan scan -p model/model/ | tee "$REPORT_TXT"

          # Export variables pour Python
          export REPORT_TXT
          export REPORT_JSON

          # Convert TXT -> JSON structuré avec severity
          python - << EOF
          import json
          import re
          import os

          txt_file = os.environ["REPORT_TXT"]
          json_file = os.environ["REPORT_JSON"]

          vulns = []

          current_severity = None
          current_source = None

          with open(txt_file, "r") as f:
              lines = f.readlines()

          for line in lines:
              line = line.strip()
              if not line:
                  continue

              # Detect severity section (CRITICAL/HIGH/MEDIUM/LOW)
              m_sev = re.match(r'- (LOW|MEDIUM|HIGH|CRITICAL):', line)
              if m_sev:
                  current_severity = m_sev.group(1)
                  continue

              # Detect source file
              m_source = re.search(r'Source: (.+)', line)
              if m_source:
                  current_source = m_source.group(1)
                  continue

              # Detect unsafe operator or parsing error
              if "Unsafe operator found" in line or "Parsing error" in line:
                  vulns.append({
                      "id": f"modelscan-{hash(line)}",
                      "cve": f"modelscan-{hash(line)}",
                      "file": current_source or "",
                      "message": line,
                      "severity": current_severity or "UNKNOWN"
                  })
                  current_severity = current_severity  # conserve la dernière severity
                  current_source = None  # reset source si on l'a utilisé

          # Générer JSON final
          output = {"vulnerabilities": vulns}

          with open(json_file, "w") as f:
              json.dump(output, f, indent=2)

          print(f"✅ ModelScan JSON with severity saved to {json_file}")


      # ---------------------------------------------------------
      # Étape 5 : Upload du rapport JSON
      # ---------------------------------------------------------
      - name: Upload ModelScan Report
        uses: actions/upload-artifact@v4
        with:
          name: modelscan-report-${{ env.TIMESTAMP }}
          path: reports/models/${{ env.TIMESTAMP }}/official/modelscan-report.json
          retention-days: 30

  # =============================================================
  # Job 3 : CVEEnrichment
  # Objectif : Enrichir les vulnérabilités du code avec la base NVD
  # Source   : Rapport Bandit + Base NVD (JSON)
  # =============================================================
  CVEEnrichment:
    name: Enrichissement CVE pour le code
    runs-on: ubuntu-latest
    needs:
    - CodeScan
    - DependencyScan
    - ModelScan

    steps:
      # ---------------------------------------------------------
      # Étape 1 : Récupération du dépôt
      # ---------------------------------------------------------
      - name: Récupérer le dépôt
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # Étape 2 : Configuration de Python
      # ---------------------------------------------------------
      - name: Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ---------------------------------------------------------
      # Étape 3 : Installation des dépendances Python
      # ---------------------------------------------------------
      - name: Installer les dépendances Python
        run: |
          python -m pip install --upgrade pip
          pip install requests tqdm tabulate

      # ---------------------------------------------------------
      # Étape 4 : Téléchargement de la base NVD 2.0 (JSON)
      # ---------------------------------------------------------
      - name: Télécharger la base NVD 2.0 (2020-2025)
        run: |
          mkdir -p nvd_db
          for YEAR in 2020 2021 2022 2023 2024 2025; do
            echo "⬇️ Téléchargement NVD $YEAR..."
            curl -L --retry 5 --retry-delay 10 \
              "https://nvd.nist.gov/feeds/json/cve/2.0/nvdcve-2.0-${YEAR}.json.gz" \
              -o "nvd_db/nvdcve-2.0-${YEAR}.json.gz"
            gunzip -f "nvd_db/nvdcve-2.0-${YEAR}.json.gz"
          done
          echo "✅ NVD 2.0 prête"

      # ---------------------------------------------------------
      # Étape 5 : Téléchargement du rapport Bandit
      # ---------------------------------------------------------
      - name: Télécharger les rapports Bandit
        uses: actions/download-artifact@v4
        with:
          name: bandit-report-${{ env.TIMESTAMP }}
          path: reports/bandit/${{ env.TIMESTAMP }}
      
      # ---------------------------------------------------------
      # Télécharger le rapport Trivy (dépendances)
      # ---------------------------------------------------------
      - name: Télécharger le rapport Trivy
        uses: actions/download-artifact@v4
        with:
          name: trivy-dependency-report-${{ env.TIMESTAMP }}
          path: reports/dependency/${{ env.TIMESTAMP }}

      # ---------------------------------------------------------
      # Étape 6 : Enrichissement CVE (code uniquement)
      # ---------------------------------------------------------
      - name: Enrichissement CVE (code + dépendances)
        run: |
          OUTPUT_DIR="reports/cve_enrichment/${{ env.TIMESTAMP }}"
          mkdir -p "$OUTPUT_DIR"

          python scripts/cve_enrichment.py \
            --nvd-db "nvd_db" \
            --bandit-report "reports/bandit/${{ env.TIMESTAMP }}" \
            --trivy-report "reports/dependency/${{ env.TIMESTAMP }}" \
            --output "$OUTPUT_DIR/enriched_cves.json"


      # ---------------------------------------------------------
      # Étape 7 : Archivage du rapport enrichi
      # ---------------------------------------------------------
      - name: Uploader le rapport CVE enrichi (code only)
        uses: actions/upload-artifact@v4
        with:
          name: enriched-code-cve-report-${{ env.TIMESTAMP }}
          path: reports/cve_enrichment/${{ env.TIMESTAMP }}/enriched_cves.json
          retention-days: 30
